import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    ratingContainer: {
        flexDirection: 'row',
        marginTop: 15
    },
    ratingValue: {
        marginLeft: 7,
        fontWeight: '500'
    }
});
