import React from 'react';
import Register from 'estore/containers/Credential/Register';

const RegisterScreen = () => {
    return (
        <Register />
    )
}
export default React.memo(RegisterScreen)