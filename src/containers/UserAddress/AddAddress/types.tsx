export enum UserAddressFields {
    firstName = "firstName",
    lastName = "lastName",
    phoneNumber = "phoneNumber",
    email = "email",
}
export type CommonType = {
    value?: string,
    isValid: boolean
}