query productByCategories($filter: Filter, $sort: Sort) {
  products(filter: $filter, sort: $sort) {
    total
    items {
      name
      id
      thumbnail
      rating
      rawDiscount
      priceBeforeDiscount
      price
      description
    }
  }
}
