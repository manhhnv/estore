query productByCategories($categoryId: Int) {
  products(filter: {categoryId: $categoryId}) {
    total
    items {
      name
      id
      thumbnail
      rating
      rawDiscount
      priceBeforeDiscount
      price
      description
    }
  }
}
