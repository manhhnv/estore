query featureProducts($limit: Int) {
  products(filter: {limit: $limit}) {
    items {
      name
      id
      thumbnail
      rating
      rawDiscount
      priceBeforeDiscount
      price
      description
    }
    total
  }
}
