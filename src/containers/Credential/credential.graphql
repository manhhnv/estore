mutation login($accessToken: String!) {
    login(accessToken: $accessToken) {
        token
        user {
            id
            firstName
            lastName
            email
            avatar
        }
    }
}